#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../glob/dist/esm/bin.mjs" "$@"
else
  exec node  "$basedir/../../../glob/dist/esm/bin.mjs" "$@"
fi
# cmd-shim-target=/root/.openclaw/workspace/fidesorigin-demo/node_modules/.pnpm/hardhat-gas-reporter@2.3.0_hardhat@2.28.6_supports-color@8.1.1_ts-node@10.9.2_@types+no_99b1b1e2b2cd1d6bc8ec7201eba38643/node_modules/glob/dist/esm/bin.mjs
