#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../ts-command-line-args/dist/write-markdown.js" "$@"
else
  exec node  "$basedir/../../../ts-command-line-args/dist/write-markdown.js" "$@"
fi
# cmd-shim-target=/root/.openclaw/workspace/fidesorigin-demo/node_modules/.pnpm/typechain@8.3.2_supports-color@8.1.1_typescript@5.9.3/node_modules/ts-command-line-args/dist/write-markdown.js
