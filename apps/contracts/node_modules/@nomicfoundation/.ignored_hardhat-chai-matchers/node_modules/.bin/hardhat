#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../hardhat/internal/cli/bootstrap.js" "$@"
else
  exec node  "$basedir/../../../../hardhat/internal/cli/bootstrap.js" "$@"
fi
# cmd-shim-target=/root/.openclaw/workspace/fidesorigin-demo/node_modules/.pnpm/@nomicfoundation+hardhat-chai-matchers@2.1.2_@nomicfoundation+hardhat-ethers@3.1.3_ethe_3ab2bc4e5460b5a68f4b9bac3469cf0e/node_modules/hardhat/internal/cli/bootstrap.js
