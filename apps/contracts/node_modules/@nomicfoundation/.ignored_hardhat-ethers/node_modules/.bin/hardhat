#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../hardhat/internal/cli/bootstrap.js" "$@"
else
  exec node  "$basedir/../../../../hardhat/internal/cli/bootstrap.js" "$@"
fi
# cmd-shim-target=/root/.openclaw/workspace/fidesorigin-demo/node_modules/.pnpm/@nomicfoundation+hardhat-ethers@3.1.3_ethers@6.16.0_hardhat@2.28.6_supports-color@8.1.1_8afd0e09471c22c9eeb5ab248a199b37/node_modules/hardhat/internal/cli/bootstrap.js
