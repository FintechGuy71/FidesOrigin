#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../hardhat/internal/cli/bootstrap.js" "$@"
else
  exec node  "$basedir/../../../../hardhat/internal/cli/bootstrap.js" "$@"
fi
# cmd-shim-target=/root/.openclaw/workspace/fidesorigin-demo/node_modules/.pnpm/@nomicfoundation+hardhat-ignition@0.15.16_@nomicfoundation+hardhat-verify@2.1.3_hardhat_638e550346ee6cf2a297a220de531aea/node_modules/hardhat/internal/cli/bootstrap.js
