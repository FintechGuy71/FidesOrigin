#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../ts-node/dist/bin-transpile.js" "$@"
else
  exec node  "$basedir/../../../../ts-node/dist/bin-transpile.js" "$@"
fi
# cmd-shim-target=/root/.openclaw/workspace/fidesorigin-demo/node_modules/.pnpm/@nomicfoundation+hardhat-toolbox@6.1.2_e33f7205bdcadc0a526ff877465872d0/node_modules/ts-node/dist/bin-transpile.js
