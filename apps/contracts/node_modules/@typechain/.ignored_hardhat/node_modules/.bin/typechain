#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../typechain/dist/cli/cli.js" "$@"
else
  exec node  "$basedir/../../../../typechain/dist/cli/cli.js" "$@"
fi
# cmd-shim-target=/root/.openclaw/workspace/fidesorigin-demo/node_modules/.pnpm/@typechain+hardhat@9.1.0_@typechain+ethers-v6@0.5.1_ethers@6.16.0_typechain@8.3.2_suppo_40efa8b6078737dfb4539346fbe51033/node_modules/typechain/dist/cli/cli.js
