#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../assemblyscript/bin/asc" "$@"
else
  exec node  "$basedir/../../../../assemblyscript/bin/asc" "$@"
fi
# cmd-shim-target=/root/.openclaw/workspace/fidesorigin-demo/node_modules/.pnpm/@graphprotocol+graph-cli@0.73.0_@types+node@22.19.21_encoding@0.1.13_node-fetch@2.7.0_e_35b50e212284be1f89582de0f6a5d6b8/node_modules/assemblyscript/bin/asc
