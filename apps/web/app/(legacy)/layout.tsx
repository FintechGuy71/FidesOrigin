import "@/css/legacy.css";

/* Legacy pages group — scopes the classic stylesheet to classic routes. */
export default function LegacyGroupLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
