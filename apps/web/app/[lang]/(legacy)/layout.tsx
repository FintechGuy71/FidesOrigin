import "@/css/legacy.css";

/* Localized legacy pages group — scopes the classic stylesheet. */
export default function LocalizedLegacyGroupLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
